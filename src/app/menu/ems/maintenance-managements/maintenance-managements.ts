import { ChangeDetectionStrategy, Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-maintenance-managements',
  imports: [RouterModule],
  templateUrl: './maintenance-managements.html',
  styleUrl: './maintenance-managements.scss',
})
export class MaintenanceManagements { }
